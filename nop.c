#include "monty.h"

/**
  * nop - Does nothing
  * @stack : Pointer to stack struct
  * @line_num : Line in which the function is called
*/

void nop(stack_t **stack, unsigned int line_num)
{
	(void) stack;
	(void) line_num;
}
